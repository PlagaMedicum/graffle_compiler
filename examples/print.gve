f(v) = g+1
    if true
        <<< v
    else <<< "Else what?"
    end
    g = v + 1
end.

cycle_test(a)
    while a not equals to 0
        print a
        input a
    end
end

v = V("SAS") @ vertice
<<< v

e = "Start" -[50]-> "End" @ edge
<<< e

g = G("OK.", v, e, (v -- v)) @ graph
g = g + ((v <- "2"), false)
<<< g

cycle_test(1)

a = 3 * "2" + 6. <<< a
>>> b
if b
    is "1"
        <<< b + 11
    is '2'
        <<< 11 + b
    default
        <<< b
end

<<< f(a)

<<< "" and 5

<<< !false